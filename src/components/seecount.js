import React from 'react'
import PropTypes from 'prop-types'

const SeeCount = () => (
  <p>How many times did U click ? {console.log(this.state.total)}</p>
)


SeeCount.propTypes = {

}

export default SeeCount
